# Tor Hidden Service Configuration for ZigZag

# Run as daemon: no (Docker manages the process)
RunAsDaemon 0

# SOCKSPort disabled — we only host a hidden service
SocksPort 0

# Hidden Service config
HiddenServiceDir /var/lib/tor/hidden_service
HiddenServicePort 80 nginx:80

# Security settings
HardwareAccel 1
SafeLogging 1
Log notice stdout
